use smartcrab::prelude::*;

use crate::dto::discord::DiscordNotification;

/// Input layer that receives HTTP requests and converts them to Discord notifications.
pub struct HttpInput {
    pub target_channel_id: String,
}

impl Layer for HttpInput {
    fn name(&self) -> &str {
        "HttpInput"
    }
}

#[async_trait]
impl InputLayer for HttpInput {
    type Output = DiscordNotification;

    async fn run(&self) -> Result<DiscordNotification> {
        todo!("Implement HTTP server (axum/actix-web) to accept webhook payloads")
    }
}
