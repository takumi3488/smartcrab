use smartcrab::prelude::*;

use crate::dto::discord::DiscordMessage;

/// Input layer that receives Discord messages (mentions and DMs).
pub struct DiscordInput;

impl Layer for DiscordInput {
    fn name(&self) -> &str {
        "DiscordInput"
    }
}

#[async_trait]
impl InputLayer for DiscordInput {
    type Output = DiscordMessage;

    async fn run(&self) -> Result<DiscordMessage> {
        todo!("Implement Discord message listener (serenity/twilight gateway)")
    }
}
